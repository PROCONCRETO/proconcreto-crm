# Módulo: Producción

## Archivos

- `js/ordenes-produccion.js` (288 líneas) — órdenes de servicio y pipeline de producción
- `js/produccion-diaria.js` (277 líneas) — registro de producción del día, más Inventario de Producto Terminado
- `js/produccion-materia-prima.js` — Materia Prima e Inventario de Cemento por Bodega (movida de Calidad, ver "Materia Prima" más abajo)

## Datos

- Tablas Supabase: `ordenes_servicio`, `producciones`, `materia_prima`

## Pantallas

`pipeline-produccion`, `ordenes-servicio`, `produccion-diaria`, `inventario`, `materia-prima`

## Qué hace

Convierte una cotización aprobada en una orden de servicio, hace seguimiento del pipeline de producción, registra la producción diaria de la planta y las recepciones de materia prima (con control de inventario de cemento por bodega).

## Encadenamiento con Logística

Una orden puede traer un desglose por producto (`items[]`, cuando viene de una cotización aceptada vía `crearOrdenDesdeCotizacion()`) o solo una cantidad genérica (órdenes creadas a mano con `cantidad`+`descripcion`) — `_itemsDeOrden()` (en `logistica.js`) unifica ambos casos.

Al editar una orden (`editarOrden()`), se muestra una tabla de **Pedido / Entregado / Saldo** por producto (`renderSaldoOrden()`), cruzando con las entregas de Logística vinculadas a esa orden y marcadas "Hecha" en Cumplidos (`_cantidadEntregadaPorProducto()`). Así se ve de un vistazo si una orden ya se entregó completa, parcial, o sigue pendiente.

**Pasar la orden a Logística (2026-08-29)**: la pantalla "📦 Órdenes de Despacho" de Logística (`js/logistica-ordenes-despacho.js`, ver `docs/modulos/logistica.md`) lista, sin salir de este módulo ni tener que buscarla a mano, las órdenes ya listas o con algo de inventario disponible, con un botón que abre directo el modal de "Nuevo Viaje" con la orden ya vinculada. Es una vista calculada, no toca `ORDENES` ni el estado de la orden — la transición a "Despachado" sigue siendo 100% manual desde este tablero.

## Materia Prima (movida de Calidad, 2026-08-19)

La pantalla Materia Prima (recepciones de cemento, arena, grava, agua, aditivos...) vivía en Calidad — el usuario pidió moverla a Producción. Cambio puramente de navegación: mismo archivo renombrado (`js/calidad-materia-prima.js` → `js/produccion-materia-prima.js`), mismo CRUD contra la tabla `materia_prima`, sin cambios de esquema por la mudanza en sí. El botón "🧱 Materia Prima" pasó del subnav de Calidad al de Producción (después de "📦 Inventario"); `ir('materia-prima')` no cambió, solo el subnav donde vive el botón.

Junto con la mudanza, el usuario pidió 3 ajustes al formulario de registro:

- **Lote ya no es obligatorio** — antes bloqueaba guardar junto con Proveedor; ahora solo Proveedor es obligatorio (`guardarMateriaPrima()`).
- **Columna Bodega en la tabla general** (2026-08-20, a pedido del usuario): la tabla de Materia Prima (`renderMateriaPrima()`) muestra la Bodega de cada registro de Cemento (`_BODEGAS_CEMENTO[m.bodegaCemento]`) — "—" para el resto de tipos, que no tienen bodega.
- **Proveedor de Cemento es un desplegable fijo** (`ALION`/`ARGOS`, `_PROVEEDORES_CEMENTO`) en vez de texto libre — solo cuando Tipo = Cemento (`_alCambiarTipoMateriaPrima()` alterna entre el `<select id="m-mp-proveedor-cemento">` y el `<input id="m-mp-proveedor">` de texto libre que se sigue usando para el resto de tipos, ya que no hay una lista fija confirmada para Arena/Grava/Aditivo). `guardarMateriaPrima()` lee del campo que esté visible según el tipo.
- **Bodega** (`bodegaCemento`, antes llamada "Fuente/Silo" en la primera versión de este control — renombrada a pedido del usuario): mismo campo condicional que ya existía, solo cambió el nombre visible y el de la variable.

## Inventario de Cemento por Bodega (2026-08-19)

El usuario pidió llevar control del cemento como insumo, con dos objetivos: (1) saber cuándo pedir más, y (2) comparar el consumo real contra el teórico por producto. Las 3 bodegas reales de la planta (`_BODEGAS_CEMENTO`, `js/produccion-materia-prima.js`): **Silo 1 — Pretensados** (`silo1`), **Silo 2 — Vibrocompactados** (`silo2`), **Cemento en bolsa** (`bolsa`).

- **Entradas**: cuando el Tipo de un registro de Materia Prima es "Cemento", el modal muestra el campo **Bodega** (obligatorio, una de las 3 opciones) y **fija la Unidad a `kg`** (deshabilitada, no oculta — necesario para poder sumar entradas y salidas en una sola unidad sin adivinar cuántos kg trae un "bulto" ni convertir toneladas a mano).
- **Salidas**: cada registro de Producción Diaria puede llevar, además de la cantidad producida, el **Consumo total de cemento (kg)** de esa colada y su **Bodega** (`consumoCemento`/`bodegaCemento`, ambos opcionales — no bloquean guardar si se dejan vacíos, igual que Orden/Observaciones) — mismas 3 opciones que Materia Prima, para que el inventario sume entradas y salidas del mismo lado.
- **Stock e indicador de reorden**: `calcularInventarioCemento()` (`js/produccion-materia-prima.js`) suma entradas de `MATERIA_PRIMA` (tipo Cemento, no rechazadas) menos salidas de `PRODUCCIONES`, por cada una de las 3 bodegas. El stock actual se muestra junto al **consumo promedio de los últimos 30 días** y los **días de cobertura estimados** (stock ÷ consumo promedio), este último solo informativo.
- **Color de cada tarjeta por stock actual, no por días de cobertura** (2026-08-19, a pedido del usuario, corrigiendo el criterio inicial): `_colorStockCemento(stock)` pinta el fondo completo de la tarjeta según el kg disponible — 🔴 rojo hasta 15.000 kg (`_KG_STOCK_ROJO`), 🟡 ámbar de 15.000 a 30.000 kg (`_KG_STOCK_AMBAR`), 🟢 verde por encima de 30.000 kg. Los días de cobertura se dejaron solo como dato informativo (texto), ya no deciden el color — con poco consumo reciente una bodega puede salir con muchos "días de cobertura" aunque el stock en kg ya sea crítico (caso real: Silo 2 con 3.401 kg y "sin consumo reciente" no se pintaba en rojo con el criterio anterior).

### El rojo y el ámbar del semáforo se confundían — corregido con el validador de color (2026-09-09)

El usuario reportó, con una captura, que el rojo crítico se veía muy parecido al ámbar y no se distinguía el semáforo de un vistazo — el primer ajuste de esta pantalla (2026-08-30, `#D50000` rojo vs `#E65100` ámbar) no fue suficiente. Se verificó con el validador de color de la skill `dataviz` (`scripts/validate_palette.js`, distancia ΔE en OKLab — no a ojo): ese par medía **ΔE 9.6 en visión normal**, por debajo del piso de 15 que el validador marca como "difícil de distinguir incluso con visión de color completa" — confirmaba el reporte del usuario con un número, no solo intuición.

- Corregido a **rojo `#D32F2F` / ámbar `#FF8F00`** — el ámbar se corrió hacia un dorado más amarillo (más lejos del rojo en el círculo de color; el `#E65100` original era en realidad un naranja-rojizo, muy cerca del rojo en tono) y el rojo a un tono más puro. Con este par, la distancia sube a **ΔE 21.7** — claramente por encima del piso.
- El contraste de cada color contra el fondo de su propia tarjeta se revisó por separado: rojo (`#D32F2F` sobre `#FFEBEE`) y verde (`#2E7D32` sobre `#E8F5E9`) pasan holgado (≥3:1); el ámbar (`#FF8F00` sobre `#FFF3E0`) queda en 2.23:1 — por debajo del 3:1 recomendado, aceptado como una concesión menor porque el dato más importante de la tarjeta (el stock en kg, la cifra grande) **nunca** usa este color — siempre es texto neutro oscuro — y el ámbar solo tiñe el rótulo y "días de cobertura", en negrita.
- El verde no se tocó (no fue parte del reporte, y cambiarlo se habría alejado más del resto de la app sin necesidad).
- Verificado con el validador (no a ojo) y con una captura de pantalla real reproduciendo el escenario exacto reportado (Silo 1 en rojo, Silo 2 en ámbar, Cemento en bolsa en rojo) confirmando que ahora se distinguen con claridad.
- **`renderInventarioCemento()`** se llama al final de `renderMateriaPrima()`, así que se mantiene al día solo: tanto `materia_prima` como `producciones` ya disparan `rerenderPantallaActiva()` (ver `js/datos-realtime.js`), que vuelve a llamar `renderMateriaPrima()` sin importar cuál de las dos tablas cambió — no hizo falta ninguna suscripción realtime nueva.
- **No se corrige historial**: los registros de Materia Prima/Producción Diaria de antes de este cambio no tienen `bodegaCemento`/`consumoCemento` — quedan fuera de la suma (el inventario arranca a contar desde que se empiece a usar el campo nuevo, no se le pide al usuario que complete a mano lo viejo).

## Consumo de cemento por registro — real vs. teórico (2026-08-19)

- **Real vs. teórico**: la tabla de Producción Diaria muestra, en la columna "Cemento", el consumo real registrado y — si ese producto tiene un Costeo de Producto guardado — el % de variación contra el teórico (`_celdaCementoProduccion()`, usa `_cementoTeoricoPorUnidad(producto)` de `js/costeo-producto.js`, que recalcula el Costeo guardado de ese producto y lee la cantidad de la fila de Materia Prima marcada `esCemento: true`, kg por unidad × cantidad producida = teórico total). Verde si la variación es ≤10%, ámbar ≤25%, rojo por encima — para detectar sobreconsumo de un vistazo. Sin Costeo de Producto guardado para ese producto, se muestra solo el real, sin badge de comparación (no bloquea nada — hoy solo una parte del catálogo tiene Costeo guardado).
- `_cementoTeoricoPorUnidad()` funciona para los 3 tipos de estructura de Costeo (Vibrocompactado/Reforzado/Pretensado) porque el flag `esCemento` se agregó en los 3 `_MATERIALES_COSTEO_PESO.forEach()` que arman `materiaPrimaDetalle` — cambio puramente aditivo, no afecta ningún cálculo existente.

## Merma y Segundas — deficiencias del proceso de vibrocompactado (2026-09-09)

A pedido del usuario: "en la producciones diarias de vibrocompactados, llevamos datos de merma de producto (huecos en las placas o ciclos que se descartan desde la máquina y producto de mala calidad) y segundas (productos con deficiencias menores de calidad)... incluyamos este espacio en las producciones para poder llevar una estadística de estas deficiencias del proceso."

- Cada registro de Producción Diaria gana dos campos numéricos opcionales, junto al selector de Producto: **Merma** y **Segundas** (`m-prod-merma`/`m-prod-segundas`, campos `merma`/`segundas` del registro) — no bloquean guardar si se dejan vacíos, mismo criterio que Orden/Observaciones/Consumo de cemento. No están restringidos a ningún grupo de producto en particular (se dejan siempre visibles, igual que Consumo de cemento/Bodega) — el usuario decide si aplican o no según lo que esté registrando.

### Reorganización del modal y buscador de Producto (2026-09-09)

Junto con agregar Merma/Segundas, el usuario pidió tres ajustes al modal "Registrar/Editar Producción":

- **Alineación**: las etiquetas largas de Merma/Segundas (con su explicación entre paréntesis) quedaban de distinto alto en la misma fila y desalineaban los campos de abajo. Se acortaron a solo "Merma"/"Segundas" (la explicación pasó a `title`, tooltip al pasar el mouse) y se les fijó `min-height` a las tres etiquetas de la fila (Cantidad producida/Merma/Segundas) — mismo patrón ya usado en otros modales del aplicativo (ej. Control de Ensayos) para este problema exacto.
- **Reordenado**: "Orden de producción asociada" subió a la fila donde antes estaba "Cantidad producida" (ahora junto a Fecha), y "Cantidad producida" bajó a compartir fila con Merma y Segundas (`form-grid-3`, tres columnas) — quedan las tres cantidades del lote juntas.
- **Producto buscable**: el `<select>` nativo (agrupaba por tipo con `<optgroup>`, pero no se podía filtrar escribiendo) se reemplazó por un buscador con resultados flotantes — mismo patrón visual que el buscador de Cliente/Producto del modal de Viaje en Logística (`filtrarClienteEntrega()`/`filtrarProductosEntrega()`, `js/logistica.js`; ver `docs/modulos/logistica.md`), con una diferencia a propósito: acá el desplegable con TODOS los productos (agrupados por tipo, igual que antes) aparece apenas se hace foco en el campo vacío — no hace falta escribir nada para "desplegar y elegir" como con el `<select>` — mientras que el buscador de Logística solo muestra algo a partir de 2 caracteres escritos. La búsqueda filtra por nombre o código. El campo de texto conserva el id `m-prod-producto` y guarda el nombre elegido directo en su `.value` (igual que hacía el `<select>`), así que `guardarProduccion()`/`abrirModalProduccion()`/`editarProduccion()` no necesitaron ningún cambio para leerlo — se removió `poblarSelectProductos()` (ya no aplica) y se agregó el listener global de "clic afuera cierra el desplegable" que ya usa Logística para sus propios buscadores.
- Verificado con un arnés real — 17 aserciones (el listado completo se despliega en foco vacío, filtra por nombre y por código, mensaje de "sin resultados", elegir un producto fija el valor y cierra el desplegable, clic afuera lo cierra y clic adentro no, `guardarProduccion()` sigue guardando el producto elegido correctamente, y `editarProduccion()` deja el buscador cerrado y con el producto correcto al abrir) — más una captura de pantalla real confirmando la alineación y el reordenado — sin errores de consola.
- **"Cantidad producida" sigue significando SOLO unidades de primera** — merma y segundas son campos aparte, nunca se suman a `cantidad`. Esto es intencional y confirmado explícitamente por el usuario, porque `cantidad` alimenta `calcularInventario()` (inventario de planta) y el consumo de cemento por unidad (ver abajo) — mezclar ahí piezas descartadas o de segunda distorsionaría ambos.
- En la tabla, la celda de Cantidad muestra una sub-línea compacta con la merma/segundas de ese registro cuando tiene (`_subLineaMermaSegundas()`), mismo patrón que el badge de cemento real-vs-teórico — no ocupa columna aparte.
- El resumen de la pantalla (`#prod-diaria-resumen`, ya filtrable por fecha/tipo de producto/búsqueda) gana tres tarjetas nuevas, todas respetando los mismos filtros activos:
  - **Merma** y **Segundas**: totales de unidades.
  - **% Deficiencia**: `(merma + segundas) / (cantidad + merma + segundas) × 100` — semáforo (verde ≤5%, ámbar ≤10%, rojo por encima).
  - **Cemento / unidad (primera)**: `consumo total de cemento / SOLO unidades de primera (cantidad)` — a pedido explícito del usuario: "sigamoslo calculando sobre lo producido (unidades de primera)... resultado de dividir el consumo total de cemento entre las unidades de primera producidas" — el divisor **nunca** incluye merma ni segundas. Distinto del badge "real vs. teórico" de la sección anterior (que compara un registro puntual contra el Costeo de Producto guardado) — esta tarjeta es un promedio real agregado sobre el filtro activo, sin depender de que exista ningún Costeo guardado. Se oculta (muestra "—") si no hay cantidad o consumo de cemento en el filtro.
- Verificado con un arnés real — 16 aserciones (totales de merma/segundas correctos, % deficiencia calculado sobre cantidad+merma+segundas, cemento por unidad calculado SOLO sobre unidades de primera aunque haya merma/segundas en el filtro, sub-línea en la tabla, los campos arrancan en blanco en un registro nuevo y se recuperan al editar uno existente, y `guardarProduccion()` guarda merma/segundas correctamente — incluido el caso de dejarlos vacíos, que no debe bloquear ni guardar `NaN`) — sin errores de consola.

## Dashboard "Estadísticas de Producción" — v1 Vibrocompactados (2026-09-09)

Nueva pantalla dentro del subnav de Producción (`📊 Estadísticas`, `js/estadisticas-produccion.js`), a pedido del usuario: "hagamos ahora un dashboard de estadísticas de producción... empecemos con uno para vibrocompactados". Replica el mismo lenguaje visual que `estadisticas-logistica.js` — reusa directamente sus funciones ya globales `_tarjetaKPI()`, `_colorSemaforo()`, `_colorCapacidad()`, y copia su selector de período en ventana rodante (`setPeriodoLogistica()` → `setPeriodoProduccion()`: 7/30/90 días o Todo, `_periodoProduccion`, botones `est-prod-btn-*`) en vez del patrón de Todo/Anual/Mensual con dropdowns de año/mes que usan Histórico/Pipeline (ese está atado a `COTIZACIONES`, no aplica acá).

### "Vibrocompactados" se define por el Costeo de Producto, no por el catálogo

El catálogo no marca qué `grupo` (Adoquín, Bloque, Prelosa, Viga Pretensada...) es vibrocompactado — el usuario indicó explícitamente usar el Costeo de Producto: **"básate en los costeos, los cuales sí están para vibrocompactados discriminados con mayor claridad"** / **"tómalo por el TIPO dado en los costeos"**.

- `_mapaTipoEstructuraPorProducto()` cruza `CATALOGO` (código↔nombre — se usa `CATALOGO`, no `PRODUCTOS`, para que un producto ya descontinuado en el catálogo activo se siga pudiendo clasificar) con `COSTEO_PRODUCTOS` (código↔`tipoEstructura`, ver `docs/modulos/costeo.md`) para armar un mapa `{nombreProducto: tipoEstructura}` una sola vez por render. Ojo: el campo se llama `tipoEstructura`, no `tipo`.
- `COSTEO_PRODUCTOS` se carga para **cualquier** usuario autenticado (no está gateado a Centro de Costos — solo la pantalla de edición lo está), así que este dashboard puede clasificar sin depender de que quien lo mire tenga acceso a Centro de Costos.
- **No todo producto vibrocompactado tiene todavía un Costeo guardado.** Los registros de `PRODUCCIONES` que no se pueden clasificar así **no se adivinan por `grupo`** — se excluyen de las tarjetas y gráficas, y se cuentan aparte en una nota naranja bajo las tarjetas ("N registros de productos sin Costeo de Producto guardado no se incluyen..."), para que el hueco de cobertura sea visible en vez de silencioso, y para animar a completar el Costeo de los productos que falten.

### Contenido del dashboard

- **Tarjetas KPI** (`#est-prod-tarjetas`): Ciclos de producción, **Ciclos promedio / día** (2026-09-09 — `totalCiclos / días con ciclos`, dividido entre los días que de verdad tuvieron producción, no entre los días corridos del período — mismo criterio de "solo días de producción" que ya usan las gráficas de tendencia; un fin de semana o un día sin producción no debería diluir el promedio), Unidades de primera producidas, Merma (ud), Segundas (ud), % Deficiencia (semáforo, mismos umbrales ya usados en Producción Diaria: verde ≤5%, ámbar ≤10%, rojo por encima — `_colorDeficiencia()`), Cemento / unidad (primera) — mismas fórmulas ya establecidas en la sección "Merma y Segundas" de arriba, agregadas sobre el período filtrado.
- **Tendencia de producción** (`_chartTendenciaProduccion()`): línea de una sola serie, en **ciclos** (no unidades) por día, omitiendo los días sin producción — mismo mecanismo que `_chartTendencia()` de Logística. Ver la sección "Ciclos, no unidades" abajo. **Tooltip con desglose por producto, por renglones** (2026-09-09, a pedido del usuario: "cuando toquemos un punto, incluyamos el producto que se fabricó y la cantidad de primera"; luego afinado a un formato exacto: "escribe en este orden y por renglones: fecha / producto / ciclos / unidades") — un día de la vista general puede sumar ciclos de varios productos a la vez, así que el número agregado solo no dice cuál se fabricó ni cuánto. El título del tooltip es la fecha sola; el cuerpo agrega, por cada producto fabricado ese día (ordenados de mayor a menor ciclos), tres renglones separados: `Producto: X`, `Ciclos: N`, `Unidades: M`. Guardado en `detalleD[]`, paralelo a `labels`/`ciclosD` — se arma junto con el resto del recorrido día por día, no aparte.
- **Tendencia de merma y segundas** (`_chartTendenciaDeficiencia()`) — **gráfica aparte, con su propio eje**: al principio Merma/Segundas compartían eje con Primera en una sola gráfica de 3 series, pero al ser números mucho más chicos quedaban pegadas al piso, casi ilegibles. Separarlas en su propia gráfica (rojo `#d03b3b` merma, ámbar `#fab219` segundas — misma paleta de estado fija que ya usa `estadisticas-logistica.js`) deja ver su tendencia real. Usa un criterio de "día con actividad" propio y distinto al de la gráfica de arriba — omite los días sin NINGUNA merma/segundas, aunque ese día sí haya habido producción (no tendría sentido ensuciar esta gráfica con ceros de días donde simplemente no hubo ningún problema). Se mantiene en unidades (no ciclos) — el usuario no pidió ese cambio acá. **Mismo tooltip por renglones y por producto** que la gráfica de Ciclos ("haz lo mismo en la gráfica de mermas y segundas") — acá el equivalente de "ciclos" es Merma%/Segundas% (lo que esta gráfica sí mide): `Producto: X`, `Merma: N%`, `Segundas: M%`, `Unidades: P`, por cada producto que ese día tuvo algo de merma o segundas (uno en 0%/0% no aporta nada al tooltip). Como son 2 series (Merma y Segundas) en la misma gráfica, con `interaction: { mode: 'index', intersect: false }` (para que el tooltip se active completo sin importar de cuál línea esté más cerca el mouse) el desglose se arma una sola vez desde el dataset 0 (Merma) — el `label` callback del dataset 1 (Segundas) devuelve un arreglo vacío a propósito, para no duplicar el mismo desglose dos veces en el mismo tooltip.
  - **En % frente a la producción de primera** (2026-09-09, a pedido del usuario: "muéstralas en términos porcentuales frente a la producción de primera"): cada punto es `merma o segundas del día / PRIMERA del día × 100` — el divisor es solo primera, no lo intentado (distinto de la tarjeta "% Deficiencia", que sí divide entre lo intentado — acá el usuario pidió puntualmente "frente a la producción de primera"). Un día sin nada de primera no tiene con qué calcular el %, se omite igual que un día sin ninguna merma/segundas.
- **Ranking de productos por ciclos de producción** (`_chartRankingVolumen()`): top 10, barra horizontal azul — mismo patrón que "Destinos más frecuentes" de Logística. Solo aparece en la vista general (ver filtro de producto abajo).
- **Ranking de productos por % de deficiencia** (`_chartRankingDeficiencia()`): top 10, coloreado POR BARRA según su propio semáforo (verde/ámbar/rojo) — para ver de un vistazo cuáles productos están fallando más, no solo cuánto se pierde en total. Excluye productos con menos de 5 unidades intentadas (`primera+merma+segundas`) en el período — sin ese piso, un producto con 1 unidad intentada y esa unidad de merma (100% de deficiencia) desplazaría del ranking a productos con volumen real. Solo aparece en la vista general.
- **Consumo de cemento por unidad — tendencia** (`_chartCementoPorUnidad()`): línea, kg/unidad de primera por día — mismo criterio de siempre dividir solo entre unidades de primera, omite días sin cantidad o sin consumo de cemento registrado.

### Ciclos, no unidades — la producción de referencias distintas no es comparable (2026-09-09)

A pedido del usuario: "la producción de productos de diferentes referencias no es comparable por el hecho de que la máquina no produce las mismas unidades por ciclo para cada producto... para una visión general, podemos hacerlo por número de placas diarias (o ciclos) y si filtramos algún producto, sí podemos hacerlo por ciclos y por unidades para ver el desempeño de una referencia en particular". Un ciclo de la máquina es la unidad de esfuerzo real y comparable entre productos — a diferencia de una "unidad" terminada, que puede ser 1 pieza grande o 40 chicas del mismo ciclo, según el molde.

- **`_unidadesCicloPorProducto()`**: mismo cruce CATALOGO↔COSTEO_PRODUCTOS que `_mapaTipoEstructuraPorProducto()`, pero lee `costeo.rendimiento.unidadesCiclo` — el campo "Unidades/Ciclo" del cuestionario de Vibrocompactado en Costeo de Producto (ver `docs/modulos/costeo.md`), un dato real de planta (cuántas piezas terminadas salen de un ciclo de la máquina). Un producto vibrocompactado CON Costeo guardado pero sin ese dato digitado (0 o vacío) queda fuera del mapa — no hay con qué convertir sus unidades a ciclos. Esos registros se cuentan aparte en una segunda nota (distinta de la de "sin Costeo"), y siguen contando normal en unidades/merma/segundas/cemento — solo quedan fuera de "Ciclos de producción" y del ranking de ciclos.
- `ciclos de un registro = cantidad (de primera) / unidadesCiclo de ese producto` — se suma por día para la tendencia, y por producto para el ranking.
- **Filtro de producto** (`#est-prod-filtro-producto`, se puebla solo con los productos vibrocompactados que aparecen en el período): en la **vista general** ("Todos los productos"), la tendencia y el ranking de volumen van en ciclos, y los dos rankings (volumen y deficiencia) se muestran normal. Al **filtrar a un producto específico**, los dos rankings se ocultan (comparar un producto contra sí mismo no dice nada) y aparece en su lugar una gráfica nueva, **"Unidades producidas — producto filtrado"** (`_chartUnidadesFiltrado()`, propia escala, azul) — dentro de un mismo producto, ciclos y unidades son directamente proporcionales (`unidades = ciclos × Unidades/Ciclo`), así que mostrar unidades sí tiene sentido ahí, aunque no lo tenga mezclando varias referencias.
- Verificado con un arnés real — 20 aserciones sobre el mapa de ciclos, la tarjeta "Ciclos de producción" con y sin filtro, la nota de "sin Unidades/Ciclo", el ranking en ciclos (excluye productos sin ese dato), el filtro poblándose y preservando la selección, y el toggle correcto de mostrar/ocultar rankings vs. la gráfica de unidades filtrada — más capturas de pantalla reales de ambas vistas — sin errores de consola.

### Fuera de alcance en esta v1 (a propósito)

- Comparación agregada "consumo real vs. teórico" (versión agregada del badge que ya existe por-registro en Producción Diaria) — pendiente para una siguiente vuelta si se pide.
- Selector de Tipo de estructura (Vibrocompactado/Reforzado/Pretensado) — v1 queda fijo en Vibrocompactado; el código ya separa por `tipoEstructura`, así que agregar los otros tipos después es sencillo, pero el selector todavía no se construyó.
- Ninguna tabla ni migración nueva en Supabase — todo se calcula en memoria sobre `PRODUCCIONES` + `COSTEO_PRODUCTOS` + `CATALOGO`, ya cargados.

## Filtros de Producción Diaria (2026-08-21)

A pedido del usuario, la pantalla ganó filtros para revisar el histórico sin tener que scrollear todo `PRODUCCIONES`:

- **Rango de fechas** (`filtro-fecha-desde-prod`/`filtro-fecha-hasta-prod`, ambos opcionales) — reemplaza al filtro de un solo día que había antes (`filtro-fecha-prod`). Sugerido como mejora natural sobre lo pedido: un registro de producción se suele revisar por semana/mes, no día por día.
- **Tipo de producto** (`filtro-grupo-prod`, dropdown) — filtra por `p.grupo` (el mismo campo `grupo` del catálogo que ya trae cada registro, ver `guardarProduccion()`). Poblado dinámicamente (`poblarFiltroGrupoProd()`) solo con los grupos que de verdad aparecen en `PRODUCCIONES` — mismo patrón que `poblarFiltroGrupoInv()` (Inventario de Producto Terminado, mismo archivo).
- **Producto/Responsable** (`buscar-produccion`, texto libre) — ya existía, sin cambios de comportamiento (busca coincidencia parcial en `producto` o `responsable`); solo se ajustó el placeholder para que diga explícitamente que también busca por responsable.
- Los 4 filtros son acumulables (se aplican todos los que tengan valor a la vez) y siguen alimentando el mismo resumen "Filtrado" / "Total registros" de arriba de la tabla.

## Mismos filtros en Órdenes, Inventario y Materia Prima (2026-08-21)

El usuario pidió el mismo criterio de filtros (fecha, tipo de producto, búsqueda) en el resto de pantallas del módulo — adaptado a lo que cada una realmente representa, no una copia mecánica:

- **Órdenes de Producción** (`js/ordenes-produccion.js`): `renderOrdenes()` dejó de aceptar un `lista` filtrada desde afuera (se eliminó `filtrarOrdenes(q)`, que era el único punto que lo hacía) y ahora lee sus propios filtros, igual que las demás pantallas del módulo. Rango de fechas sobre `fechaEntrega`. El "Tipo de producto" no vive directo en la orden (una orden trae `items[]` si viene de una cotización, o solo `descripcion`+`cantidad` si se creó a mano) — `poblarFiltroGrupoOrdenes()` y el filtro mismo pasan cada renglón por `_itemsDeOrden(o)` (helper ya existente en `logistica.js`, unifica los dos casos) y cruzan el nombre contra `PRODUCTOS` para sacar su `grupo`; una orden entra al filtro si CUALQUIERA de sus renglones pertenece al grupo elegido. El buscador de texto (ahora con `id="buscar-ordenes"`, antes no tenía id) no cambió: número/cliente/descripción.
- **Inventario de Producto Terminado** (`renderInventario()`, `js/produccion-diaria.js`): ya tenía Tipo de producto (`filtro-grupo-inv`) y búsqueda por producto. Se sumó rango de fechas — pero como cada fila es un TOTAL acumulado (producido − despachado a la fecha), no un movimiento puntual, filtra por **última producción** (`r.ultima`) en vez de una fecha propia de la fila; el stock mostrado sigue siendo el real acumulado, el filtro solo decide qué referencias listar.

### Solo las entregas "Hecha" descuentan inventario (2026-08-30)

El usuario preguntó directamente "¿los Hechos de entregas están descontando inventario?" — la respuesta real era **no**: `calcularInventario()` solo restaba órdenes marcadas manualmente "Despachado" en el tablero de Producción, sin ninguna relación con si sus entregas de Logística ya se habían marcado "Hecha" en Cumplidos o no. El usuario aclaró el criterio correcto: "cuando se hace una entrega, el producto ya salió de planta por tanto no se tiene en inventario. distinto a cuando se programa y no se ha hecho, que el producto no ha salido de planta por tanto sigue en inventario" — es decir, "Hecha" (no "programada") es la señal real de que algo salió de planta.

**Diseño (confirmado con el usuario, pregunta directa sobre el caso borde)**: por cada orden, `calcularInventario()` resta lo **MAYOR** entre (a) lo que sus entregas ya marcadas "Hecha" en Logística suman por producto (`_cantidadEntregadaPorProducto()`, `js/logistica.js`) y (b) si la orden sigue marcada manualmente "Despachado" en Producción, el pedido completo. (a) y (b) nunca se suman para el mismo producto — competirían por lo mismo y duplicarían el descuento; se usa `Math.max()`. El respaldo (b) existe a propósito para no perder de vista despachos que nunca se programaron como entrega en Logística (ej. un cliente que retira en planta) — quitar el estado "Despachado" del todo habría dejado ese caso sin ninguna forma de descontarse. Las entregas "Hecha" que no están vinculadas a ninguna orden (`_cantidadEntregadaSinOrdenPorNombre()`) también restan, resueltas por nombre plano de producto (`_nombrePlanoDeProducto()`, convierte la clave "código — nombre" que usan las líneas de entrega al nombre plano que usa `calcularInventario()`/`PRODUCCIONES`).

Una entrega **programada y todavía pendiente** (o reprogramada) sigue sin tocar el inventario — el material sigue físicamente en planta hasta que se confirma. Esto es intencional y ya estaba así desde que se construyó Órdenes de Despacho (ver `docs/modulos/logistica.md`) — lo único que cambió acá es qué pasa cuando SÍ se marca "Hecha".

**Historial de entradas/salidas por referencia**: en Inventario de Producto Terminado, el nombre del producto en cada fila es clickeable (`verHistorialInventario()`) y abre un modal con dos tablas — Entradas (registros de Producción Diaria de ese producto) y Salidas (el mismo desglose que usa `calcularInventario()` para su columna Despachado: entregas "Hecha" con fecha/cliente/orden, y filas aparte para el respaldo manual "Orden marcada Despachado" cuando cubre algo que sus entregas no cubrieron). La suma de "Salidas" del historial siempre coincide exacto con la columna Despachado de la tabla — se calculan con la misma lógica (`_historialInventarioProducto()`), no una aproximación aparte. La fecha de una fila manual usa la fecha de entrega estimada de la orden (`fechaEntrega`) porque no hay ningún registro exacto de cuándo se cambió el estado a mano.

Verificado ejecutando `calcularInventario()`/`_historialInventarioProducto()`/`verHistorialInventario()` reales (no solo revisión de código) contra: una orden con entrega Hecha parcial sin marcar Despachado, una marcada Despachado sin ninguna entrega (respaldo manual puro), una con ambas señales a la vez para el mismo producto (confirma que no duplica — `MAX`, no suma), una entrega Hecha 100% sin que la orden se marcara nunca Despachado (el fix pedido), una entrega vinculada pero pendiente (sigue sin restar), una entrega cancelada (no cuenta), un producto con código real del catálogo (prueba la resolución de clave "código — nombre" → nombre plano) y una entrega "Hecha" sin ninguna orden vinculada — 15 aserciones sobre el modal real, todas correctas, incluida la comprobación de que la suma de "Salidas" del historial coincide exacto con "Despachado" de la tabla.
- **Materia Prima** (`renderMateriaPrima()`, `js/produccion-materia-prima.js`): ya tenía Tipo (`filtro-tipo-mp`, tipo de material — Cemento/Arena/Grava/...) y búsqueda por proveedor/lote. Se sumó rango de fechas sobre `fechaRecepcion`.
- Mismo patrón en las 4 pantallas: los filtros son acumulables, se recalculan en cada `renderX()` (nunca se guarda estado de filtro aparte), y el mensaje de "sin resultados" distingue cuando es por un filtro activo.

### Presentación unificada — título/acción y filtros en renglones separados (2026-08-21)

Con varios filtros nuevos, el `.acciones-row` (título + controles a la derecha, `justify-content: space-between; flex-wrap: wrap`) empezó a envolver a 2 renglones en las pantallas con más filtros pero se quedaba en 1 en las que tenían menos — inconsistente según el ancho disponible, a pedido del usuario se unificó con un solo criterio: **el título y el botón de acción principal ("+ Registrar...", "+ Nueva Orden") siempre van en su propio `.acciones-row`; los filtros van en un `.flex-gap` aparte, en su propio renglón, siempre debajo** — mismas 4 pantallas (Producción Diaria, Órdenes, Inventario, Materia Prima). Inventario no tiene botón de acción, así que su `.acciones-row` queda solo con el título — el layout de `.acciones-row` (`space-between`) lo deja igual de bien alineado a la izquierda sin necesitar un caso especial.
